{
  "name": "sunday-v2-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Sunday V2 backend services - see README.md",
  "scripts": {
    "bootstrap": "npm ci --prefix _shared && npm ci --prefix api && npm ci --prefix blockchain-indexer && npm ci --prefix iot-data-collector && npm ci --prefix price-collector && npm ci --prefix price-publisher && npm ci --prefix yield-publisher",
    "test": "npm test --prefix _shared && npm test --prefix price-collector && npm test --prefix api && npm test --prefix blockchain-indexer && npm test --prefix price-publisher && npm test --prefix yield-publisher",
    "build": "sam build"
  },
  "license": "ISC"
}
