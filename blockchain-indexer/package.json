{
  "name": "@sunday-v2/blockchain-indexer",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prebuild": "rimraf built",
    "build": "tsc -p tsconfig.build.json",
    "prepack": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "author": "",
  "license": "ISC",
  "description": "Indexes YieldAdapter events into Mongo",
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "3.758.0",
    "ethers": "^6.13.5",
    "mongoose": "8.12.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.2",
    "vitest": "^3.2.4"
  }
}
