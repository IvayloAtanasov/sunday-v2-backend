{
  "name": "@sunday-v2/price-publisher",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prebuild": "rimraf built",
    "build": "tsc -p tsconfig.build.json",
    "prepack": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "author": "",
  "license": "ISC",
  "description": "Publishes realized electricity prices on chain",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.758.0",
    "@aws-sdk/client-secrets-manager": "3.758.0",
    "@aws-sdk/lib-dynamodb": "3.758.0",
    "ethers": "^6.13.5",
    "moment": "^2.29.4",
    "moment-timezone": "^0.5.43"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.2",
    "vitest": "^3.2.4"
  }
}
